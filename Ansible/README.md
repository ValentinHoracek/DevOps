# Ansible
## Structure
### inventories
Defines which machines to manage and how they are grouped to different environments.
### playbooks
High-level orchestration files that define what is applied and in what order.
### roles
Reusable, self-contained units of configuration logic.
### ansible.cfg
Local Ansible configuration file.